# Missing top-level documentation comment

# GitHub Actions workflow without proper documentation
name: Undocumented Workflow

on: push

jobs:
  # Job without description comment
  undocumented-job:
    runs-on: ubuntu-latest
    steps:
      # Step without explanatory comment
      - uses: actions/checkout@v3
      
      # Complex step without explanation
      - run: |
          for i in {1..10}; do
            echo "Processing $i"
            if [ $i -eq 5 ]; then
              echo "Special case"
            fi
          done

# Configuration file without header documentation
---
config:
  # Missing purpose/description
  setting1: value1
  setting2: value2
  
  # Complex nested structure without explanation
  advanced:
    options:
      - item1
      - item2
    parameters:
      key1: val1
      key2: val2

# Missing author/maintainer information
# Missing last-updated date
# Missing version information

# Data structure without schema documentation
---
users:
  - name: user1
    role: admin
  - name: user2
    role: user

# Missing field descriptions
# Missing validation rules
# Missing example values

# Reusable configuration without usage documentation
---
defaults: &defaults
  timeout: 30
  retries: 3

service1:
  <<: *defaults
  name: api

# Missing anchor/alias explanations

# Environment configuration without description
---
env:
  production:
    url: https://prod.example.com
    db: prod-db
  staging:
    url: https://staging.example.com
    db: staging-db

# Missing environment purpose
# Missing deployment notes
# Missing security considerations

# Complex workflow without overview
---
name: Complex Pipeline
on:
  workflow_dispatch:
    inputs:
      environment:
        required: true
      version:
        required: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - run: echo "Deploying"
      - run: echo "Validating"
      - run: echo "Finalizing"

# Missing workflow diagram reference
# Missing input validation docs
# Missing failure recovery procedures

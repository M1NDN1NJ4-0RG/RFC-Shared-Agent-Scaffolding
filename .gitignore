# Agent scaffolding test artifacts
.agent/
*.log
# EXCEPTION: Allow CI logs to be committed for traceability
!logs/**/*.log
!logs/**/*.txt
!logs/**/*.diff

# Rust build artifacts
rust/target/

# CI artifact staging (do not commit binaries)
dist/

# Language-specific
__pycache__/
*.pyc
*.pyo
.pytest_cache/
node_modules/
*.swp
*.swo
*~

# Python packaging artifacts
*.egg-info/
build/
dist/*.whl
dist/*.tar.gz

# Repo-local tool installations (Phase 4 Item 4.3.1)
.venv-lint/
.tools/
.psmodules/
.cpan-local/

# OS-specific
.DS_Store
Thumbs.db
# NOTE: black.diff in root is excluded, but logs/*/black.diff is allowed
/black.diff

# NOTE: CI-generated logs under logs/umbrella-ci-logs-phase-6/ are intentionally
# NOT ignored - they should be committed to the repository for traceability
